<% @restaurants.each do |restaurant| %>
  <div>
    <%= restaurant.name %>
    <%= restaurant.address %>
    <%= restaurant.phone_number %>
    <%= restaurant.category %>
  </div>
<% end %>

<%= link_to 'add a restaurant', new_restaurant_path %>
